<resources>
    <!-- Base application theme. -->
    <style name="Theme.AnlimWeather" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Main color -->
        <item name="colorPrimary">@color/basic_color</item>

        <!-- TextColor -->
        <item name="colorOnPrimary">@color/white</item>

        <!-- Widgets color  -->
        <item name="colorSecondary">@color/white</item>

        <!-- StatusBar and NavigationBar -->
        <item name="android:statusBarColor">@color/basic_color</item>
        <item name="android:navigationBarColor">@color/basic_color</item>
    </style>

    <!-- Start screen theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:statusBarColor">@color/basic_color</item>
        <item name="android:navigationBarColor">@color/basic_color</item>
    </style>
</resources>