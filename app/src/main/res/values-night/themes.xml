<resources>
    <!-- Base application theme. -->
    <style name="Theme.AnlimWeather" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Main color -->
        <item name="colorPrimary">@color/basic_color</item>

        <!-- TextColor -->
        <item name="colorOnPrimary">@color/white</item>

        <!-- Widgets color  -->
        <item name="colorSecondary">@color/basic_color</item>

        <!-- StatusBar and NavigationBar -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>
</resources>